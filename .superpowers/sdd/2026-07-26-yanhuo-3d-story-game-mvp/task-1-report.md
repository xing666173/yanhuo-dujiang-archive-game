# Task 1 Report: Reproducible Test and Preview Foundation

## Implementation Summary

Added the dependency-free static preview server, the requested npm scripts and pinned development dependencies, project ignore entries, and a behavior-level unit test covering homepage serving and encoded path traversal rejection.

## Files Changed

- `.gitignore`: preserved `.worktrees/` and added the six required project entries.
- `package.json`: added the requested scripts and pinned `@playwright/test` `1.61.1` and `three` `0.185.1`.
- `package-lock.json`: generated by `npm install` with the requested dependency versions.
- `tools/serve.cjs`: added `createStaticServer({ rootDir })` and the `preview` entry point.
- `tests/unit/static-server.unit.test.cjs`: added the server behavior test.

## RED Verification

Command:

```powershell
node --test tests/unit/static-server.unit.test.cjs
```

Output:

```text
Error: Cannot find module '../../tools/serve.cjs'
Require stack:
- C:\Users\axezt\Desktop\暑假社会实践\site\.worktrees\yanhuo-3d-story-game\tests\unit\static-server.unit.test.cjs
...
✖ tests\unit\static-server.unit.test.cjs
ℹ tests 1
ℹ pass 0
ℹ fail 1
```

This failed as expected because the behavior test was written before `tools/serve.cjs` existed. The failure was a missing production module, not a test assertion or syntax error.

## GREEN and Full-Suite Verification

Focused GREEN command:

```powershell
node --test tests/unit/static-server.unit.test.cjs
```

Output:

```text
✔ serves the homepage and rejects path traversal (111.8156ms)
ℹ tests 1
ℹ suites 0
ℹ pass 1
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

Full current unit file set command:

```powershell
node --test tests/unit/*.unit.test.cjs
```

Output:

```text
✔ serves the homepage and rejects path traversal (58.4822ms)
ℹ tests 1
ℹ suites 0
ℹ pass 1
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

The specified npm command was also run:

```powershell
npm run test:unit
```

It failed in this Windows environment because the installed Node runtime resolves `node --test tests/unit` as a module path and reports `Cannot find module '...\\tests\\unit'`. The package script remains verbatim from the task brief; the file-glob command above verifies all current unit test files.

## Self-Review

- The test exercises HTTP behavior through a real local server and `fetch`; it does not inspect source text.
- `/` maps to `index.html` and returns an HTML content type.
- Encoded traversal is decoded before validation and returns `403`.
- The resolved path remains constrained to `rootDir`.
- Unknown files return `404`; known extensions receive explicit MIME types and `Cache-Control: no-store`.
- Only the Task 1 files were added or changed.

## Concerns

- `npm run test:unit` is not runnable as written on the current Windows Node setup because `node --test tests/unit` does not discover the directory here. The required script was kept unchanged per the brief. A future task should decide whether to revise that script to an explicit glob or use a runtime where directory discovery works.
- `npm run test:e2e` is configured but no Playwright tests or configuration were part of Task 1, so it was not run.

## Fix Round 1

### Finding Addressed

Critical: `npm run test:unit` is non-runnable in the reported Windows environment because package.json targets the `tests/unit` directory; therefore `npm test` cannot pass.

### Changed Files

- `package.json`: changed only `scripts.test:unit` from directory discovery to two quoted Node file globs:
  `node --test "tests/unit/**/*.unit.test.cjs" "tests/unit/**/*.unit.test.mjs"`.
- `.superpowers/sdd/2026-07-26-yanhuo-3d-story-game-mvp/task-1-report.md`: appended this fix-round report.

No deferred Minor items were addressed.

### TDD and Root-Cause Evidence

The old command failure is recorded above: `npm run test:unit` invoked `node --test tests/unit` and failed with `Cannot find module '...\\tests\\unit'` on the reported Windows Node environment.

The first candidate glob was tested and rejected because it ran zero tests:

```powershell
D:\node\npm.cmd run test:unit
```

```text
> test:unit
> node --test "tests/unit/**/*.unit.{cjs,mjs}"

ℹ tests 0
ℹ suites 0
ℹ pass 0
ℹ fail 0
```

The second candidate was also tested and rejected because its suffix did not match the existing `*.unit.test.cjs` filename:

```powershell
D:\node\npm.cmd run test:unit
```

```text
> test:unit
> node --test "tests/unit/**/*.unit.cjs" "tests/unit/**/*.unit.mjs"

ℹ tests 0
ℹ suites 0
ℹ pass 0
ℹ fail 0
```

The final configuration matches the current naming convention and keeps a separate `.mjs` glob for future tests.

### Passing Verification

Npm command required by the fix round:

```powershell
D:\node\npm.cmd run test:unit
```

Output:

```text
> test:unit
> node --test "tests/unit/**/*.unit.test.cjs" "tests/unit/**/*.unit.test.mjs"

✔ serves the homepage and rejects path traversal (46.9813ms)
ℹ tests 1
ℹ suites 0
ℹ pass 1
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

Focused direct test:

```powershell
C:\Users\axezt\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe --test tests/unit/static-server.unit.test.cjs
```

Output:

```text
✔ serves the homepage and rejects path traversal (48.7856ms)
ℹ tests 1
ℹ suites 0
ℹ pass 1
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```
